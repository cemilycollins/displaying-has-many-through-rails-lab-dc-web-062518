<h1>Here are all your appointments</h1>

<% @doctor.appointments.each do |app| %>
  <p><%= app.date_time + " at " + app.appointment_datetime.strftime("%H:%M")%></p>
  <p><%= link_to app.patient.name, patient_path(app.patient) %></p><br>
<% end %>
