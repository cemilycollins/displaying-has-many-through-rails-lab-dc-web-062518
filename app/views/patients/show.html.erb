<h1>Here are your appointments</h1>

<% @patient.appointments.each do |app| %>
  <p><%= app.date_time + " at " + app.appointment_datetime.strftime("%H:%M")%></p>
  <p><%= link_to app.doctor.name, doctor_path(app.doctor) %></p><br>
<% end %>
